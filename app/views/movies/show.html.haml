-# in app/views/movies/show.html.haml

%h2 Details about #{@movie.title}

%ul#details
  %li
    Rating:
    = @movie.rating
  %li
    Released on:
    = @movie.release_date.strftime("%B %d, %Y")
  %li
    Director is:
    = @movie.director

%h3 Description:

%p#description= @movie.description

= link_to 'Edit', edit_movie_path(@movie)
= button_to 'Delete', movie_path(@movie), :method => :delete, :confirm => 'Are you sure?'
= link_to 'Back to movie list', movies_path
%br
= link_to 'Find_Movies_With_Same_Director', dir_path(:movie_director => @movie.director, :movie_title => @movie.title), :id => 'Find Movies With Same Director'

-#%a#Find_Movies_With_Same_Director{:href=>"/dir?movie_title=#{@movie.title}&movie_director=#{@movie.director}"}Find_Movies_With_Same_Director

-#I don't like this:
-#= link_to 'Find Movies With Same Director', movies_path(:sort => @movie.director), :id => 'Find Movies With Same Director'
-# instead, I use  <a href=""></a>

